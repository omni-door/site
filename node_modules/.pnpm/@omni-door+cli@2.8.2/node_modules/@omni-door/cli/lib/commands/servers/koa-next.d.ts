/// <reference types="node" />
import type { ProxyConfig, MiddlewareConfig, CorsConfig } from '../dev/server';
import type { NextRouter } from '../../index.d';
export interface KNServerParams {
    dev: boolean;
    proxyConfig?: ProxyConfig;
    middlewareConfig?: MiddlewareConfig;
    corsConfig?: CorsConfig;
    ipAddress: string;
    host: string;
    listenHost?: string;
    port: number;
    httpsConfig?: {
        key?: string | Buffer;
        cert?: string | Buffer;
    };
    nextRouter?: NextRouter;
}
export default function ({ dev, ipAddress, proxyConfig, middlewareConfig, corsConfig, host, listenHost, port, httpsConfig, nextRouter }: KNServerParams): void;
