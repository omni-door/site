import ora from 'ora';
import type { SpinnerName } from 'cli-spinners';
import type { SPINNER_STATE } from './global.d';
export declare class Spinner {
    private spinner;
    private _state;
    constructor();
    state(state: SPINNER_STATE, text?: string): void;
    private _decorateText;
    text(text: string): void;
    color(color: ora.Color): void;
    prefix(prefix: SpinnerName): void;
}
declare const spinner: Spinner;
export default spinner;
