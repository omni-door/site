"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var path_1 = __importDefault(require("path"));
var utils_1 = require("@omni-door/utils");
var servers_1 = require("../servers");
function handleException(msg) {
    utils_1.logWarn(msg || '发生了一些未知错误！(Ops! Some unknown errors have occurred!)');
    process.exit(0);
}
function default_1(config, options) {
    return __awaiter(this, void 0, void 0, function () {
        var e_1, server, _a, port, host, serverType, rest, p, h, ip, ipAddress, CWD, _port, _host, ServerStartCli;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    _b.trys.push([0, 2, , 3]);
                    // node version pre-check
                    return [4 /*yield*/, utils_1.nodeVersionCheck('8')];
                case 1:
                    // node version pre-check
                    _b.sent();
                    return [3 /*break*/, 3];
                case 2:
                    e_1 = _b.sent();
                    utils_1.logWarn(e_1);
                    return [3 /*break*/, 3];
                case 3:
                    if (!config || JSON.stringify(config) === '{}') {
                        handleException('请先初始化项目！(Please initialize project first!)');
                    }
                    server = config.server;
                    if (!server || JSON.stringify(server) === '{}') {
                        handleException('配置文件 start 字段缺失！(The start field is missing in config file!)');
                    }
                    _a = server || {}, port = _a.port, host = _a.host, serverType = _a.serverType, rest = __rest(_a, ["port", "host", "serverType"]);
                    if (!serverType) {
                        handleException('请指定 server 类型！(Please specify server-type!)');
                    }
                    p = options.port;
                    h = options.hostname;
                    ip = utils_1.requireCwd('ip');
                    ipAddress = ip.address();
                    CWD = process.cwd();
                    _port = (p ? +p : port) || 6200;
                    _host = h || host || '0.0.0.0';
                    ServerStartCli = {
                        next: path_1.default.resolve(CWD, 'node_modules/.bin/next') + " start --port " + _port + " --hostname " + _host
                    };
                    switch (serverType) {
                        case 'koa-next':
                            servers_1.KNServer(__assign({ dev: process.env.NODE_ENV === 'development', port: _port, host: _host, ipAddress: ipAddress }, rest));
                            break;
                        case 'next':
                            utils_1.exec([ServerStartCli[serverType]]);
                            break;
                        case 'koa-nuxt':
                        case 'nuxt':
                        default:
                            utils_1.logWarn('暂不支持 ssr-vue 项目!');
                    }
                    return [2 /*return*/];
            }
        });
    });
}
exports.default = default_1;
